name: Check Everything Before Publish

on:
  pull_request:

permissions:
  actions: read
  contents: read

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"

      - name: Derive appropriate SHAs for base and head for `nx affected` commands
        uses: nrwl/nx-set-shas@v4

      - name: Install deps
        run: pnpm install

      - name: Run Checks/Lint/Build
        run: npx nx affected -t lint build

      - name: Run unit tests with coverage
        run: pnpm exec nx run-many -t test --all --coverage --passWithNoTests

      - name: Run integration tests
        run: pnpm exec nx affected -t test-integration

      - name: Install Playwright browsers
        run: pnpm --filter @family-tree/web exec playwright install --with-deps chromium

      - name: Run E2E tests
        run: pnpm exec nx affected -t test-e2e

      - name: Code Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: pnpm exec nx affected -t scan --parallel 1
